[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "solrat"
dynamic = ["version"]
description = "Solar Radiative Transfer non-LTE forward-modeling code."
authors = [{ name = "Ivan I. Yakovkin" }]
license = { text = "MIT" }
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "numpy>=2.0",
    "pandas",
    "matplotlib",
    "tqdm",
    "sympy",
    "scipy",
    "coloredlogs",
    "typing_extensions>=4.0.0; python_version<'3.11'",
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[project.urls]
Homepage = "https://github.com/yakovkinii/solrat"

[project.optional-dependencies]
dev = [
    "black",
    "isort",
    "flake8",
    "Flake8-pyproject",
    "pytest",
    "coverage[toml]",
    "build",
    "packaging",
]

[tool.setuptools.dynamic]
version = { attr = "solrat.about.version" }

[tool.setuptools.packages.find]
where = ["."]
include = ["solrat*"]

[tool.setuptools.package-data]
solrat = [
    "py.typed",
    "atom_model/multi_term_atom_model/data/**/*.csv",
]

[tool.black]
line-length = 120
target-version = ["py39"]

[tool.isort]
profile = "black"
line_length = 120
py_version = 39

[tool.flake8]
max-line-length = 120
extend-ignore = ["E203", "W503"]
exclude = [
    ".git",
    ".venv",
    ".pytest_cache",
    ".mypy_cache",
    ".pyright",
    "build",
    "dist",
    "docs",
    ".github",
    ".gitignore",
    "*.egg-info",
]

[tool.pytest.ini_options]
testpaths = ["_tests"]
addopts = "-q"

[tool.coverage.run]
source = ["solrat"]
branch = true
omit = [
    # Pure matplotlib rendering helper (no scientific logic); exercised by the demos, not unit-tested.
    "solrat/atom_model/shared/utility/plot_stokes_profiles.py",
]

[tool.coverage.report]
fail_under = 95
show_missing = true
skip_covered = false

[tool.coverage.xml]
output = "coverage.xml"
